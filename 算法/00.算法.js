function fn(arr = []) {
    // O(n)
    for (let i = 0; i < arr.length; i++) {
        console.log(arr);
    }
}

function fn1(params) {
    // O(n^2)
    for (let i = 0; i < arr.length; i++) {
        console.log(arr);
        for (let i = 0; i < arr.length; i++) {
            console.log(arr);
        }
    }
}

function fn1(params) {
    // O(logn)
    // 二分
}

function fn2(array) {
    // O(n*logn)
    for (let i = 0; i < array.length; i++) {
        // 二分
    }
}
